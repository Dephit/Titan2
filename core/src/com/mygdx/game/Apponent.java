package com.mygdx.game;

/*
import com.badlogic.gdx.graphics.Color;
import com.badlogic.gdx.graphics.g2d.Batch;
import com.badlogic.gdx.scenes.scene2d.Actor;

import java.util.Comparator;
import java.util.function.Function;
import java.util.function.ToDoubleFunction;
import java.util.function.ToIntFunction;
import java.util.function.ToLongFunction;

public class Apponent extends Actor {

    int place, birthYaer;

    float squatFirst, squatSecond, squatThird, squatBest, squatOrderedNext,
            benchFirst, benchSecond, benchThird, benchBest, benchOrderedNext,
            deadliftFirst, deadliftSecond, deadliftThird, deadliftBest, deadliftOrderedNext,
            total, totalBest;



    String name, surname, team;

    Apponent(int birthYaer, float squatBest, float benchBest, float deadliftBest, float bestTotal, String name, String surname, String team) {
        this.birthYaer = birthYaer;
        this.squatBest = squatBest;
        this.benchBest = benchBest;
        this.deadliftBest = deadliftBest;
        this.totalBest = bestTotal;
        this.name = name;
        this.surname = surname;
        this.team = team;
    }

    public Apponent() {

    }

    @Override
    public void draw(Batch batch, float parentAlpha) {
        super.draw(batch, parentAlpha);
        MyMethods.textDrawing(batch, place  + "   " + surname + "   " + name + "   " + birthYaer + "   " + squatBest + "   " + benchBest + "   " + deadliftBest + "   " + totalBest, getX(), getY(), 2, Color.WHITE);
    }

    @Override
    public void act(float delta) {
        super.act(delta);
    }

}
*/
